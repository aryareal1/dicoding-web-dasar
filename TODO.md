# TODO List

## Mandatory Criteria
- [x] Use `<header>`.
- [x] Use `<nav>`.
- [x] Use `<footer>`.
- [x] Use `<main>`.
- [x] Use `<article>`.
- [x] Use `<aside>`.
- [x] Add `<a>` link to my dicoding profile in `<nav>`.
- [x] Add image in `<aside>`.
- [x] Use **flexbox**.

## Tips
- [x] Use **relative pathing**.
- [x] Refer to external files correctly.
- [x] Use **IDE** (VSCode).

## Suggestions
- [x] Separate styling as external `.css` file.
- [x] Add `alt` into images.
- [x] Use **heading** correctly (`<h1>` must be used once).
- [x] Use `@media screen`.
- [x] Add little bit of `.js`.